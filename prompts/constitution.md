Set these once at the start of your project to prevent the AI from using outdated React patterns:
- We always use the Next.js App Router (not the Pages Router).
- Server Components are preferred for data fetching; Client Components only when interactivity is needed.
- Use Tailwind CSS for all styling.
- Strict TypeScript is mandatory for all components and API routes.
